trigger: none

pool:
  name: 'Default'

variables:
  webAppName: 'webapp-$(Build.BuildId)'
  resourceGroupName: 'techish-webapp-rg'
  location: 'East US'
  postgresServerName: 'webapp-postgres'
  databaseName: 'webappdb'
  adminUsername: $(DBuser)
  adminPassword: '$(DBpass)'  # Set in pipeline variables

stages:
- stage: Setup
  jobs:
  - job: InstallDependencies
    displayName: 'Install Required Utilities'
    steps:
    - script: sudo apt-get update
      displayName: 'Update package list'
    - script: sudo apt-get install -y curl gnupg wget software-properties-common
      displayName: 'Install basic utilities'
    - script: |
        if ! command -v node &> /dev/null || ! command -v npm &> /dev/null; then
          echo "Installing Node.js 18..."
          curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
          sudo apt-get install -y nodejs
        else
          echo "Node.js and npm are already installed"
        fi
      displayName: 'Check and Install Node.js 18'
    - script: |
        if ! command -v az &> /dev/null; then
          echo "Installing Azure CLI..."
          curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
        else
          echo "Azure CLI is already installed"
        fi
      displayName: 'Check and Install Azure CLI'
    - script: |
        if ! command -v psql &> /dev/null; then
          echo "Installing PostgreSQL Client..."
          sudo apt-get install -y postgresql-client
        else
          echo "PostgreSQL client is already installed"
        fi
      displayName: 'Check and Install PostgreSQL Client'
    - script: |
        if ! command -v terraform &> /dev/null; then
          echo "Installing Terraform..."
          wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com jammy main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
          sudo apt-get update && sudo apt-get install -y terraform
        else
          echo "Terraform is already installed"
        fi
      displayName: 'Check and Install Terraform'

- stage: Infrastructure
  dependsOn: Setup
  jobs:
  - job: DeployInfra
    steps:
    - task: TerraformTaskV2@2
      inputs:
        provider: 'azurerm'
        command: 'init'
        workingDirectory: 'infra'
    - task: TerraformTaskV2@2
      inputs:
        provider: 'azurerm'
        command: 'plan'
        workingDirectory: 'infra'
        environmentServiceNameAzureRM: 'azure-sp'
        commandOptions: '-input=false -var="admin_password=$(adminPassword)"'
        runAzLogin: true
    - task: TerraformTaskV2@2
      inputs:
        provider: 'azurerm'
        command: 'apply'
        workingDirectory: 'infra'
        environmentServiceNameAzureRM: 'azure-sp'
        commandOptions: '-input=false -var="admin_password=$(adminPassword)"'

- stage: Database
  dependsOn: Infrastructure
  jobs:
  - job: SetupDB
    steps:
    - task: AzureCLI@2
      inputs:
        azureSubscription: 'azure-sp'  # Set service connection
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          psql "host=$(postgresServerName).postgres.database.azure.com port=5432 dbname=$(databaseName) user=$(adminUsername)@$(postgresServerName) password=$(adminPassword) sslmode=require" -f db/schema.sql

- stage: WebApp
  dependsOn: Database
  jobs:
  - job: DeployWebApp
    steps:
    - script: npm install
      workingDirectory: webapp
    - script: node -e "console.log('##vso[task.setvariable variable=sonarProjectKey]' + require('crypto').randomUUID())"
      displayName: 'Generate Sonar Project GUID'
    - task: SonarCloudPrepare@4
      inputs:
        SonarCloud: 'sonar-connection'
        organization: 'techishprod'
        projectKey: $(sonarProjectKey)
        projectName: 'WebApp'
        extraProperties: |
          sonar.sources=webapp
          sonar.tests=webapp
          sonar.test.inclusions=**/*.test.js
          sonar.javascript.lcov.reportPaths=webapp/coverage/lcov.info
      displayName: 'Prepare SonarCloud analysis'
    - script: npm test
      workingDirectory: webapp
      displayName: 'Run Unit Tests'
    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: 'webapp'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/webapp.zip'
      displayName: 'Archive WebApp'
    - task: SonarCloudAnalyze@4
      displayName: 'Run SonarCloud analysis'
    - task: SonarCloudPublish@4
      displayName: 'Publish Quality Gate Result'
    - task: PublishBuildArtifacts@1
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)'
        ArtifactName: 'webapp'
        publishLocation: 'Container'
      condition: eq(variables['SonarCloudQualityGatePassed'], 'True')
      displayName: 'Publish Artifact'
    - task: AzureWebApp@1
      inputs:
        azureSubscription: 'azure-sp'
        appType: 'webAppLinux'
        appName: $(webAppName)
        package: '$(Build.ArtifactStagingDirectory)/webapp.zip'
        runtimeStack: 'NODE|18-lts'
        appSettings: |
          DATABASE_URL=postgresql://$(adminUsername)@$(postgresServerName):$(adminPassword)@$(postgresServerName).postgres.database.azure.com:5432/$(databaseName)?sslmode=require
      condition: eq(variables['SonarCloudQualityGatePassed'], 'True')
      displayName: 'Deploy to Azure Web App'

- stage: Selenium
  dependsOn: WebApp
  jobs:
  - job: RunTests
    steps:
    - script: npm install
      workingDirectory: tests
    - script: npm test
      workingDirectory: tests
      env:
        APP_URL: 'https://$(webAppName).azurewebsites.net'